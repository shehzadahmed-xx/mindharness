// mindharness/parliament-habit — many-model parliament plugin
// Service: ctx.habit — powered by its own LLM per cordis.patch.yml llm config
// Competes in Layer 4 Workspace via salience × utility, winner broadcast
export const service = { id: 'ctx.habit', create: () => ({}) };
