// mindharness/parliament-workspace — many-model parliament plugin
// Service: ctx.workspace — powered by its own LLM per cordis.patch.yml llm config
// Competes in Layer 4 Workspace via salience × utility, winner broadcast
export const service = { id: 'ctx.workspace', create: () => ({}) };
