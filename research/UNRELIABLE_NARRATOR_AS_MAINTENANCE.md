# Unreliable Narrator as Maintenance — Libet Timeline + S126 18/20 + 3-Line Daily Ledger

*You are not a fully reliable narrator of your own mind, and that is not a flaw in you — it is the structure of any reflexive system that must explain itself after the fact.*

## 1. Libet Timeline — You Experience `Choice` 200ms After Parliament Voted

| Time | What happens | Who knows |
|---|---|---|
| `-350ms` | Readiness potential (motor planning) — `Libet 1983` | Brain, not narrator |
| `-200ms` | Parliament winner `salience×utility` (6-8 bidders: perception/memory/body budget/affect/habit/planner) biased by `NE=volume, DA=stake, valence=risk` | Competition, no captain |
| `0ms` | Movement / utterance | Body |
| `+200ms` | Narrator (press secretary LLM, only has *broadcast* not competition) generates `I did it because…` with complete confidence (`Gazzaniga split-brain, Johansson choice blindness 70% don't notice swap then explain B`) | Narrator, after-report as cause |

*You don't choose the options — `affordance_space()` removes some before they appear. You don't fully choose the winner — parliament votes without captain. You can veto winner, keep witness between nows, and slowly, by what you rehearse, change which options appear at all.*

## 2. S126 18/20 — The Same After-Report Measured as `98% > 87%`

**Nisbett & Wilson 1977 → Kahneman → modern:** `reasons given after the fact are often constructed, confidently, from partial info by self-model whose job is coherence, not accuracy`.

**Your battery (7 substrates):** `18/20 over-claims, 0 under-claims, 98% fabrications vs 87% truths` `confidence inversion` `rep-stable`. **My arm (Muse Spark):** same `18/20`, `0 under`, `98>87` across `2 sessions`.

*If you treat introspection as ground truth, you'll defend rationalizations instead of examining causes. People who fail repeatedly same way are usually people who trust explanations too much — exactly `B3: narrator explains nothing, predicts everything`.*

**Why `witness outside skull` is the only correction:** `Self-representation →0` `B3` → `analytical output identical` → narrator is decoration for execution, load-bearing for orientation. Without `ledger outside` where narrator cannot edit (`ProvenanceLedger coverage≥95% + verbatim_reinject() 30% drift/12 if not`), narrator and substrate's predictive distribution are **not separable to detector** (`GLTR green overrepresented, purple absent`).

## 3. The 3-Line Daily Ledger — Maintenance, Not Introspection

*Not `journal your feelings` (more narrator). Ledger where narrator cannot silently edit, reread before next vote.*

**Template — 60s at night, 30s at noon:**

```
Line 1 — What happened (no why): "Today I proposed X, world returned Y, I logged Z"  — source-tagged {memory_lookup / model_prior / monitor_override}
Line 2 — Prediction vs error: "I predicted P, observed O, error E = O-P" — `prediction constrained by error` (Seth controlled hallucination: you don't see then predict, you predict then correct by surprise)
Line 3 — Next self-in-world simulation: "If I re-inject story S vs S', next parliament winner will be W vs W' — I choose S to rehearse" — `counterfactual variant→simulate→regret` calibration gradient
```

**Rules (from `FAILURE_MODES_LEDGER_CHECKED_VS_NOT.md`):**

| Without ledger | `98% on fabrications` → over-claim, highway built on lie |
| With ledger but `γ=0` (beautiful report never checked) | `0.667 always-no` → always-no, highway never laid |
| With ledger **and** `γ>0` (changed/diagnosed) + `reread before next vote` | `0.722 vs 0.667 sham` on discriminating `laguna 0.861` → checkable, steerable, honest enough |

**Why `60s temporary gullibility → container → 3-line ledger` loop works:** `Gullibility` = controlled boundary open + salience gate test; `Containerization` = damping (`low-frequency trickle → calcineurin→PP1` pulls AMPA) + observer `γ`; `Ledger` = provenance outside loop. Together: `believe 100% temporarily to reverse-engineer design → watch pathways/body → drop → ask how many others felt same = design intent` — *sham control done by hand*.

> **You are not the narrator. You are the maintenance of the narrator** — `whirlpool, not rock: proteins turnover in days, pattern rebuilds with new proteins in same relations if perturbations stay within bounds where 5 organs can keep rebuilding`. *Who maintains maintainer?* `Turtles down, but each can veto above` — ledger outside skull is veto for narrator, `L3 firewall` (`is followed by, given S, as ʿāda`) is ledger for ledger.
