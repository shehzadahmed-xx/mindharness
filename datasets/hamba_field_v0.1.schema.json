{
  "schema": "springfish.dataset/hamba_field",
  "version": "0.1",
  "status": "SCHEMA_ONLY_NO_DATA_YET",
  "immutability": "append-only; weekly sha256 of the day-file; corrections create 0.2+; frozen SIM outputs are NEVER edited - comparison tables only",
  "consumers": [
    "three-cow pilot deviation tracking vs HAMBA_PILOT_001_FROZEN_CONTRACT.json",
    "first observed-action dataset feeding empirical-frequency rules (with human-pilot data)",
    "identity-profile realised-rate estimation (CP-01/CP-02)"
  ],
  "row_fields": {
    "record_id": "string, monotonic, assigned at capture",
    "cow_id": "enum: HAMBA-A|HAMBA-B|HAMBA-C",
    "cp_id": "enum from hamba/CHOICE_POINT_REGISTER.csv (CP-01..CP-10) or FREEFORM with justification",
    "observation_required": "echo of register observation text at capture time",
    "observed_value": "typed value or structured list per cp_id options_prelisted",
    "expected_frozen_ref": "contract parameter + value assumed (e.g. annual_feed_vet_per_animal_bdt=25000)",
    "deviation_note": "string, required when observed differs from expected_frozen_ref",
    "operator": "pseudonym of recorder",
    "session_timestamp": "ISO-8601 Asia/Dhaka",
    "evidence_grade": "constant OBSERVED-FIELD",
    "prohibition_ack": "constant C11_NEVER_OPERATE on rows touching constitutions"
  },
  "prohibited_columns": [
    "any column mutating or reinterpreting frozen contract values",
    "llm_output fields - local-LLM wording stays in its own INFO-diagnostic store",
    "canonical-ledger identifiers"
  ]
}
