# Proxy 5×60 — distributed opensource smallest world gain

**Where 5 is fractal every scale needs new 5 whether code or weights → society of steps needs harness at n+1**

- 5 friends ×60 seeds each =300 +2 days rotate 100/day =960 144/arm 20 RPM ~60min per friend
- Each runs: python3 -u experiments/exp_s126_v3.py --api-key $OR_KEY --base-url https://openrouter.ai/api/v1 --model minimax/minimax-m3:free --seeds 60 --out-dir lab_runs_proxy_FRIEND --exploratory
- Merge: cat lab_runs_proxy_*/results_partial.json → lab_runs_s126_minimax_n12_proxy_merged 960 manifest.json every 10 records
- Witnessed outside loop where narrator cannot edit 49ea02 coverage≥95% → sham-controlled Δ+0.0338 vs -0.010 cbedb522 7/7 → scale 964/2000
- Barzakh 874dcc 20 seeds 0.05 vs 0.76 all_pass true 0 calls tonight cheapest while Go plan 0 credits

Instructions: share agents/README.md + /tmp/resume_sham_midnight.sh with 5 friends, each runs 60, returns lab_runs_proxy_FRIEND, merge via python3 tools/merge_proxy.py
